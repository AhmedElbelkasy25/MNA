@model Models.Lesson

@{
    ViewData["Title"] = "Create Lesson";
}

<!-- Page Title -->
<h1 class="text-center mb-4" style="color: #28a745; text-shadow: 2px 2px 5px rgba(0, 0, 0, 0.5);">
    <i class="bi bi-journal-plus"></i> Create Lesson
</h1>

<!-- Card Container for Form -->
<div class="container d-flex justify-content-center">
    <div class="card shadow-lg border-0 rounded-4 p-4" style="max-width: 500px; width: 100%;">
        <div class="card-body">
            <h4 class="text-center text-success fw-bold mb-3">Lesson Details</h4>
            <hr />

            <!-- Form for Lesson Creation -->
            <form asp-action="Create" enctype="multipart/form-data">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                <!-- Title -->
                <div class="mb-3">
                    <label asp-for="Title" class="form-label fw-bold"><i class="bi bi-file-earmark-text"></i> Title</label>
                    <input asp-for="Title" class="form-control rounded-pill" placeholder="Enter lesson title" />
                    <span asp-validation-for="Title" class="text-danger"></span>
                </div>

                <!-- Content -->
                <div class="mb-3">
                    <label asp-for="Content" class="form-label fw-bold"><i class="bi bi-file-earmark-play"></i> Upload Video</label>
                    <input name="file" type="file" class="form-control rounded-pill" accept="video/*" />
                    <span asp-validation-for="Content" class="text-danger"></span>
                </div>

                <!-- Section -->
                <div class="mb-3">
                    <label asp-for="SectionId" class="form-label fw-bold"><i class="bi bi-collection"></i> Section</label>
                    <select asp-for="SectionId" class="form-control rounded-pill">
                        @foreach (var item in ViewBag.Sections)
                        {
                            <option value="@item.Id">@item.Title</option>
                        }
                    </select>
                </div>

                <!-- Submit Button -->
                <div class="text-center mt-4">
                    <button type="submit" class="btn btn-success btn-lg rounded-pill w-100">
                        <i class="bi bi-check-circle"></i> Create Lesson
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>

<!-- Back to List Button -->
<div class="text-center mt-4">
    <a asp-action="Index" class="btn btn-outline-success px-4 py-2 rounded-pill shadow-sm text-decoration-none">
        <i class="bi bi-arrow-left"></i> Back to Lessons
    </a>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
